apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
    name: cu-pagos-pay-ms-router
    namespace: apis
spec:
    hosts:
        - $HOST
    gateways:
        - apis/api-gateway
    http:
        - match:
              - uri:
                    prefix: /pagos
          route:
              - destination:
                    host: $SERVICIO
                    port:
                        number: 8080
